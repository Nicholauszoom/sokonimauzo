<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Task Report</title>
</head>
<body>
    <h1>Task Report</h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Title</th>
                <th>Description</th>
                <th>Due Date</th>
                <th>Completed</th>
            </tr>
        </thead>
        <tbody>
         {% for task in report %}
            <tr>
                <td>{{ task.id }}</td>
                <td>{{ task.name }}</td>
                <td>{{ task.description }}</td>
                <td>{{ task.start_at|date('d/m/Y') }}</td>
                <td>{{ task.status ? 'Yes' : 'No' }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>